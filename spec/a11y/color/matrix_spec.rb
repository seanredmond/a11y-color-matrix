require "spec_helper"

RSpec.describe A11y::Color::Matrix do
  it "has a version number" do
    expect(A11y::Color::Matrix::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
